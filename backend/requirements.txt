fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
psycopg2-binary>=2.9
python-dotenv>=1.0
anthropic>=0.42
sse-starlette>=2.1
python-multipart>=0.0.12
slack-sdk>=3.31
openpyxl>=3.1
google-genai>=0.3
# Shared Monte Carlo deal-waterfall engine (run_simulation MCP tool) --
# also used by deal_scenario_modeler, so a chat-triggered run and a
# webpage-triggered run are guaranteed to run identical simulation code.
# Pulls numpy/pydantic transitively; pin an explicit tag deliberately
# (not @master) -- see that repo's own README.
deal_sim_engine @ git+https://github.com/rom-ionpacific/deal_sim_engine.git@v0.2.0
# MCP server framework (+ future PII scrubber) — shared infra repo.
# Local dev: pip install -e ../../claude_enterprise_utils  (pulls mcp transitively)
# Render: once the repo is pushed (private -> needs a deploy key), enable:
#   claude_enterprise_utils @ git+https://github.com/rom-ionpacific/claude_enterprise_utils.git@main
